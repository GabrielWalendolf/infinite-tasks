{
  "name": "infinite-tasks",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild ts/main.ts --bundle --outfile=dist/main.js --platform=browser",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^6.0.3"
  }
}
